rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all access to this database.
    // This is a critical security best practice for unused databases.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
