// src/app/(public)/subscribe/success/layout.tsx
import type { ReactNode } from 'react';

export default function SubscriptionSuccessLayout({
    children,
}: {
    children: ReactNode;
}) {
    return <>{children}</>;
}
