// src/app/(public)/subscribe/cancel/layout.tsx
import type { ReactNode } from 'react';

export default function SubscriptionCancelLayout({
    children,
}: {
    children: ReactNode;
}) {
    return <>{children}</>;
}
