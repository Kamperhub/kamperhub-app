// This page was part of a feature that has been removed.
// It's kept as a minimal component to prevent build errors if the file isn't deleted by the tooling.
// Ideally, this file and its containing directory would be removed from the project.
export default function ImportantInfoPage() {
  return null;
}
