
// This page is no longer actively used in the primary user flow
// due to the shift to a trial-based auto-upgrade model managed externally (e.g., Stripe).
// It can likely be deleted from the project.
// Kept as a minimal component to prevent build errors if not deleted by tooling.

export default function SubscribePage() {
  return null;
}
